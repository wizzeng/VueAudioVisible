export type DrawColor = {
    segment: string;
    passSegment: string;
    timeline: string;
}


export type ProgressChangePlayload = {
    progress: number;
    current: number;
    duration: number
}
